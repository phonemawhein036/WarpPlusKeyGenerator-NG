name: Generate WARP+ Key
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Generator
        run: |
          curl -sL https://raw.githubusercontent.com/aliirsyaad/warp-plus-generator/main/warp.py -o warp.py
          python3 warp.py
